input {
  file {
    path => "/var/log/apache2/access.log"  # O ajusta la ruta de tu log según sea necesario
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  grok {
    match => { "message" => "%{COMMONAPACHELOG}" }
  }

  date {
    match => [ "timestamp", "dd/MMM/yyyy:HH:mm:ss Z" ]
  }

  grok {
    match => { "message" => '%{IPV4:clientip} - - \[%{HTTPDATE:timestamp}\] "%{WORD:verb} %{URIPATHPARAM:request}( HTTP/%{NUMBER:http_version})?" %{NUMBER:response} %{NUMBER:bytes} "(?P<referrer>[^"]*)" "(?P<>  }

  # geoip {
  #   source => "clientip"
  #   target => "geoip"
  #   database => "/usr/share/logstash/GeoLite2-City.mmdb"
  #   fields => ["country_name", "city_name", "location"]
  # }
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    user => "elastic"
    password => "Ivancasillas36"
    ssl => true
    ssl_certificate_verification => false
    index => "apache-logs-%{+YYYY.MM.dd}"
  }

  stdout { codec => rubydebug }
}
